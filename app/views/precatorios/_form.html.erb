<%= form_for(@precatorio) do |f| %>
  <% if @precatorio.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@precatorio.errors.count, "error") %> prohibited this precatorio from being saved:</h2>

      <ul>
      <% @precatorio.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
